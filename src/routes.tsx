const routes = {
    timerDashboard: {
        main: "/timer-dashboard"
    },
    auth: {
        register: "/register",
        login: "/login",
        verifyEmail: "/verify-email/:token"
    }
}

export default routes;